---

- hosts: all
  become: true
  tasks: 
  
  - name: updating yum repositories
    yum:
      update_cache: yes
    when: ansible_pkg_mgr == "yum"
    
  - name: updating apt repositories
    apt:
      update_cache: yes
    when: ansible_pkg_mgr == "apt"

  - name: installing net-tools package for yum
    yum:
      name: net-tools
      state: latest
    when: ansible_pkg_mgr == "yum"
  
  - name: installing net-tools package for apt
    apt:
      name: net-tools
      state: latest 
    when: ansible_pkg_mgr == "apt"
